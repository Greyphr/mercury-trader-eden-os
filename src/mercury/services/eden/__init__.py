"""Eden OS agent-mesh integration.

Outbound-only WebSocket client that embeds Mercury Trader into Eden's agent
mesh (``device_terminals`` server). See ``service.py`` for the service and
the Phase 1 plan (``mercury-agent-mesh-phase1.md``) for the overall design.
"""
